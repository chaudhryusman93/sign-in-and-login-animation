import 'package:flutter/material.dart';

const Color login_bg = Colors.blueGrey;
const Color signup_bg = Colors.lightBlue;

const double defaultPadding = 16.0;
const Duration defaultDuration = Duration(milliseconds: 500);
